{
  "name": "json-server-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 10000 --no-static"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}